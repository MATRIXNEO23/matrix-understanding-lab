# CP43 source design and repair boundary

Immutable source: CP42 TRAIN v2 at main/270a0729c20ede548fe373f488029241fcbb35a3, compressed SHA b6acf41e6634c2f89e6b8251fb5f816ec60fb0ca1cd6c90dfcbed46c2c12584c. The current Supervisor request authorizes separate repair, full audit, deterministic reproduction and publication, then STOP. It forbids training/model preparation/quantization/ONNX/Frozen and DEV as training. No prior "do not correct" CP42 delivery instruction authorizes changes to the source; only this separate candidate is repaired.

repairs.json declares exact source row IDs/text, field changes, preserved decisions, reasons and quarantine decisions. Labels are not inferred using regex/similarity. build.py applies this ledger, normalizes status representation, verifies full contextual/semantic equality before alias-based redundancy reduction, and adds explicit authored.py declarations. The full source payload remains unchanged. Every source row, field change and alias is retained. No generic-language flood, class quota, loss weighting, gate change, evaluator or decoder modification.

The independent audit scans all rows/claims, uses the frozen invariant validator and pure target constructor with diagnostic offsets, compares before/after duplicates/census, validates semantic acceptance invariants and source/candidate immutability, and compares all bytes to independent reproduction. Full claim review records are separate audit evidence. Automated checks do not assert measured model generalization.

New UNKNOWN act examples model genuinely damaged speech-transcript evidence; UNKNOWN kind models unresolved hearsay versus inference; UNKNOWN time explicitly contrasts past versus future; ambiguous roles have materially plausible candidates. KNOWN is encoded RESOLVED under the frozen registry. Status/alternative consumption remains G03, not silently fixed by introducing a neural head. G04 held-out readiness and G07 protected statistical overlap remain separately blocked.

Revision 1 was locked, then four missing temporal spans were found in new code-switch declarations. It is archived intact with a supplemental FAIL finding. Revision 2 has a distinct identity/version; only those four semantic rows changed, with a persisted diff. No source TRAIN v1/v2 changes. No model was loaded. Build helpers refuse overwriting any existing candidate directory.

Read the complete report for every outcome and the exact stop condition. All future work requires Supervisor authorization.
