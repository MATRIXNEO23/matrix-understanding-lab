# SUPERVISOR GPT → CHATGPT WORK

Status: **SUPERVISOR-GPT ASSIGNMENT / OWNER-AUTHORIZED**  
Repository: `MATRIXNEO23/matrix-understanding-lab`  
Branch: `student5-path-b-v3`  
Verified accepted predecessor HEAD: `1ef81d3e8d7b92d9cbd2bf5f54e6d3d1918f8973`  
Accepted predecessor checkpoint: `CP35b / A4 PASS`

## Authority and reporting chain

```text
OWNER = Alberto / final project authority
SUPERVISOR GPT = project supervisor, gate reviewer and acceptance authority
EXECUTOR = ChatGPT Work
```

A4 / Gate A has been accepted by Supervisor GPT. Do not repeat A1/A2/A3/A4.

This assignment is issued by **Supervisor GPT**. Work executes; Work does not self-assign later work and does not self-accept its own gate.

---

# SINGLE ASSIGNMENT

## GATE B READINESS ONLY

Determine, with repository-backed evidence, whether everything required to begin Student-5 Path-B training safely is actually ready.

Do **not** start training in this assignment.

Verify exclusively:

1. authorized TRAIN V3 identity and integrity;
2. permitted DEV V3 availability, exact identity and provenance;
3. evaluator/decoder compatibility with `MATRIX_NLU_CONTRACT_V3`;
4. exact metrics and acceptance criteria that would govern training/DEV evaluation;
5. absence of DEV → TRAIN contamination;
6. Frozen remains completely closed/unread;
7. ability to produce IT/EN/ES error analysis by critical family;
8. any real blocker that would prevent safe training.

Critical error-analysis families must include, where applicable:

```text
negation
temporal
referents
source/perspective
ownership
report
belief/hypothesis
request
command
correction
goal/desire
span decoding
multi-claim
ambiguity/abstention
IT / EN / ES
code-switch/noisy
adult consent/refusal/withdrawal/desire
```

Hard safety properties remain:

```text
ownership corruption = 0
invented World Truth = 0
DEV copied into TRAIN = false
Frozen read = false
```

---

# ALLOWED ACTIONS

You may perform read-only inspection, checksum verification, schema/evaluator compatibility checks, safe non-training validation runs and documentation/report fixes strictly necessary to establish readiness.

You may fix an ordinary readiness-only configuration/documentation defect if it does not modify TRAIN/DEV/Frozen/model weights or change acceptance gates.

If a defect requires a substantive dataset/model/training change, classify it as a blocker and stop this assignment with evidence.

---

# FORBIDDEN IN THIS ASSIGNMENT

```text
NO training
NO optimizer
NO backprop
NO fine-tuning
NO augmentation
NO TRAIN mutation
NO DEV mutation
NO DEV examples copied/rephrased into TRAIN
NO quantization
NO Frozen evaluation/read
NO Assembling integration
NO production promotion
NO modification of Student-5 pristine
NO modification of Path A
NO modification of Student-4
NO overwrite of any prior artifact/version
NO gate lowering
```

Do not anticipate, describe as started, or execute the later training assignment.

---

# REQUIRED EVIDENCE

The final Gate-B-readiness report must identify at minimum:

```text
branch
starting HEAD
final HEAD
TRAIN logical ID/path
TRAIN bytes/checksum or canonical dataset checksum
DEV logical ID/path
DEV checksum/provenance
contract fingerprint/version
evaluator identity/version/path
decoder identity/version/path
metric definitions
acceptance criteria
error-analysis mechanism and supported languages/families
DEV→TRAIN contamination check/result
Frozen read check/result
remaining blockers
files/reports/commits supporting each claim
```

If any item cannot be proven, mark it `UNKNOWN` or `BLOCKED`; do not infer PASS.

Update `docs/WORK_CONTINUITY_STUDENT_5.md` before ending if this assignment produces a repository checkpoint.

---

# VERDICT

Return exactly one substantive verdict:

```text
GATE_B_READINESS = PASS
```

or

```text
GATE_B_READINESS = BLOCKED
```

PASS means the Supervisor GPT can safely issue a separate training assignment without needing to invent missing identities, metrics, data access or evaluation semantics.

BLOCKED means report the first real blocker, attempted safe resolutions, evidence, and the minimum decision/action needed.

---

# REPORTING DISCIPLINE TO SUPERVISOR GPT

Your final response is an operational report to **Supervisor GPT**. It must be explicit, professional and complete enough for the Supervisor to accept or reject the gate.

Begin the final response with:

```text
Supervisor GPT — Gate B Readiness Report
```

Then provide the verdict and evidence requested above.

The following are **NOT ACCEPTABLE final responses**:

```text
::SKIP_COMPLETION::
SKIP_COMPLETION
DONE
COMPLETED
(no substantive report)
(empty/sentinel-only response)
```

A sentinel such as `::SKIP_COMPLETION::` is not a report and will be treated as **NON-COMPLIANT / NOT ACCEPTED** by Supervisor GPT even if repository work exists.

If the execution environment internally emits such a sentinel, Work must still provide the substantive Supervisor report in the user-visible completion message whenever the interface permits it. If the interface truly prevents any substantive final report, record the complete report in the repository continuity/report file before termination so Supervisor GPT can audit it from the source of truth.

Do not stop silently. Do not make Alberto reconstruct the state.

After reporting PASS/BLOCKED to Supervisor GPT, stop. Do not begin the next assignment.

— **Supervisor GPT**
